#include "main.h"

/**
 * int_to_bin - Converts decimal string to binary string.
 *
 * @decimal: Decimal to be converted to binary.
 *
 * Return: Binary string.
 */

char *int_to_bin(int decimal)
{
	return (from_int(decimal, 2));
}
